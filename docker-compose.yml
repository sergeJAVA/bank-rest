services:
  postgres:
    container_name: bankPostgresDB
    image: postgres:17.4
    restart: always
    environment:
      POSTGRES_DB: bankDB
      POSTGRES_USER: root
      POSTGRES_PASSWORD: pass
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  postgres: